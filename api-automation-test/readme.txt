Neoris

En cada Carpeta se encuentra se encuentra divido por funcionalidad dentro de Features

Para hacer la ejecución se debe tener en cuenta lo siguiente:

Login:

1) Ejecutar el Runner que se tiene como nombre TestLoginRunner

Signup:

1) Ejecutar el Runner que se tiene como nombre TestSignupRunner
2) Tener en cuenta en el archivo datasignup.csv cada vez que se debe ejecutar se debe aumentar el numero del username o colocar uno que no exista para que se pueda registrar y no genere el error de que ya existe desde la primera interacción
